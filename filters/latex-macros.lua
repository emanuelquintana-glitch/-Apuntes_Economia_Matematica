-- Filtro personalizado para macros LaTeX
function Math(elem)
    -- Procesar ecuaciones matemáticas
    return elem
end

function Pandoc(doc)
    -- Procesamiento general del documento
    return doc
end
