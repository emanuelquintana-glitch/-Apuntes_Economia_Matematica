# _metadata.yml
# Coloca este archivo en la misma carpeta que notas_completas.qmd

jupyter:
  kernelspec:
    name: python3
    language: python
    display_name: "Python 3"

execute:
  echo: true
  warning: false
  message: false
  cache: true
  freeze: auto
  daemon: false

format:
  html:
    code-fold: show
    code-tools: true
